#include <iostream> 
#include < match>
using namespace std;
int main ()
{ 
    float x , y;
    cout << "x  =";
    cin >> x;
    cout << "y = ";
    cin >> y;
 if (fabs (x < 10)&&fabs (y <5)// внутри обл
    cout << "да";
if (fabs (x > 10) || fabs (y >5)// вне обл
    cout << "нет";
    else cout << " на границе";
   return 0;
}
