#include <iostream>
#include < match>
 using namespace std;
int main ()
{ 
    float x , y;
    cout << "x  =";
    cin >> x;
    cout << "y = ";
    cin >> y;
 if (x*x +y*y <100)  // внутри обл
    cout << "да";
else if (x*x +y*y >100)  // вне обл
    cout << "нет";
    else cout << " на границе";
   return 0;
}
