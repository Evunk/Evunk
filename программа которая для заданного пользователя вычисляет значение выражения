#include <iostream>
#include <iomanip>
#include <math.h>
using namespace std;
int main()
{
    int x;
    int y;
    double z;
    cin >> x;
    cin >> y;
    z = (cos * x / 3,14 - 2 * x ) + 16 * cos x * y; 

cout << "z = " << setprecision(5) << z << endl;
return 0;

}
